node_modules/
dist/
.astro/
.env
.DS_Store
.vercel/
.vercel

# Translation working directory — extracted chunks and in-flight translations.
# Only the finished bundle in src/content/i18n/ is committed.
.i18n-work/

# OG font staging — woff2 decompressed to ttf for the rasterizer.
.og-fonts/

public/assets/og.png
# OG preview card — a build artifact of scripts/gen-og.mjs; regenerated by
# npm run build (resvg linux binary is in the lockfile, so Vercel rebuilds it).

# Playwright e2e output (npm run test:e2e)
test-results/
playwright-report/
/blob-report/
.playwright/
